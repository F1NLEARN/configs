# ============================================================
# quiz-service 로컬 개발 환경 오버라이드 (profile: local)
# ============================================================

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/quizdb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
